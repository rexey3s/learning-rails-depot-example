<tr>
<td><%= line_item.id %></td>
<td><%= line_item.product.title %></td>
<td><%= number_to_currency(line_item.price) %></td>
<td><%= line_item.quantity %></td>
<td><%= number_to_currency(line_item.total_price) %></td>
<td><%= button_to 'Delete',line_item, method: :delete, data:{ confirm: 'Are you sure to delete this line'}, class: "btn btn-danger btn-sm"%></td>
</tr>		